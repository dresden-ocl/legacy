# 2001-10-17 [sz9 ] Adjusted directories to new directory structure

rm -rf download/*

# Added mkdir to ensure existence of download directory
mkdir download

# Was: cp -a ../src/*.jar ../src/build/*.jar ../src/api download
cp  ../src/*.jar ../build/*.jar ../api download

tar c \
  -C ../api \
  . | gzip --best > download/dresden-ocl-api.tar.gz

tar c \
  -C  ../src \
  --exclude=CVS \
  --exclude=api \
  --exclude=build \
  "--exclude=*.class" \
  "--exclude=*.bak" \
  . | gzip --best > download/dresden-ocl-src.tar.gz

gunzip < download/dresden-ocl-api.tar.gz | bzip2 > download/dresden-ocl-api.tar.bz2
gunzip < download/dresden-ocl-src.tar.gz | bzip2 > download/dresden-ocl-src.tar.bz2
